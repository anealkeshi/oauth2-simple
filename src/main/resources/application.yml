server:
  port: 8080

spring:
  application:
    name: auth-service
    
#logging:
#  level:
#     org:
#       springframework:
#         security: DEBUG

logging:
  level:
     org:
       springframework:
         security:
          web:
            FilterChainProxy: DEBUG